<section class="hero">
  <div class="hero-content">
    <h1 class="hero-title">AI-Powered CFO Workspace</h1>
    <p class="hero-subtitle">Stop Flying Blind. InsightHunter delivers enterprise CFO intelligence to small businesses, freelancers, and fractional CFO firms.</p>
    <a href="/auth/register" class="btn btn-primary">Get Started Free</a>
  </div>
</section>

<style>
  .hero {
    background: radial-gradient(ellipse at bottom, var(--primary-glow), var(--bg-dark));
    padding: var(--spacing-4xl) var(--spacing-md);
    text-align: center;
  }

  .hero-content {
    max-width: var(--max-width);
    margin: 0 auto;
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin-bottom: var(--spacing-lg);
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: var(--text-secondary);
    margin-bottom: var(--spacing-2xl);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .btn {
    display: inline-block;
    border-radius: var(--border-radius-md);
    padding: var(--spacing-md) var(--spacing-xl);
    text-decoration: none;
    font-weight: var(--font-weight-bold);
  }

  .btn-primary {
    background-color: var(--primary-color);
    color: var(--bg-dark);
    box-shadow: var(--shadow-soft-glow);
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2.5rem;
    }
    .hero-subtitle {
      font-size: 1.1rem;
    }
    .hero {
        padding: var(--spacing-2xl) var(--spacing-md);
    }
  }

</style>
