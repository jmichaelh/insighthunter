---
import Layout from './../../layouts/Layout.astro';
---
<Layout title="Shop">
  
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Pricing - Insight Hunter</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    :root{--primary:#667eea;--primary-dark:#5a67d8;--secondary:#f7fafc;--success:#38a169;--warning:#ed8936;--danger:#e53e3e;--dark:#1a202c;--light:#f8f9ff;}
    *{margin:0;padding:0;box-sizing:border-box;}
    body{font-family:'Inter',sans-serif;background:var(--light);color:var(--dark);line-height:1.6;overflow-x:hidden;}
    .navbar{padding:1rem 5%;background:white;box-shadow:0 2px 10px rgba(0,0,0,.05);position:sticky;top:0;z-index:100;}
    .nav-container{max-width:1200px;margin:0 auto;display:flex;justify-content:space-between;align-items:center;}
    .logo{font-size:1.5rem;font-weight:700;color:var(--primary);}
    .cta-button{padding:0.75rem 2rem;background:var(--primary);color:white;border-radius:50px;font-weight:600;text-decoration:none;transition:all .2s;}
    .cta-button:hover{background:var(--primary-dark);transform:translateY(-1px);}
    .hero{padding:8rem 5% 4rem;background:linear-gradient(135deg,var(--primary),#764ba2);color:white;text-align:center;}
    .hero h1{font-size:clamp(2.5rem,5vw,4rem);font-weight:700;margin-bottom:1.5rem;}
    .hero-subtitle{font-size:1.25rem;max-width:600px;margin:0 auto 3rem;opacity:0.95;}
    .pricing-container{max-width:1200px;margin:0 auto;padding:0 5%;display:grid;grid-template-columns:repeat(auto-fit,minmax(350px,1fr));gap:2rem;}
    .plan-card{background:white;border-radius:24px;overflow:hidden;box-shadow:0 20px 40px rgba(0,0,0,.08);transition:all .3s;border:1px solid rgba(102,126,234,.1);}
    .plan-card:hover{transform:translateY(-10px);box-shadow:0 30px 60px rgba(0,0,0,.15);}
    .plan-header{padding:2.5rem 2rem;text-align:center;background:var(--secondary);border-bottom:1px solid rgba(0,0,0,.05);}
    .plan-name{font-size:1.25rem;font-weight:600;color:var(--dark);margin-bottom:0.25rem;}
    .plan-price{font-size:3.5rem;font-weight:700;color:var(--primary);line-height:1;}
    .plan-period{font-size:0.875rem;color:var(--text-muted);}
    .plan-features{padding:2.5rem 2rem;list-style:none;}
    .plan-features li{padding:1rem 0;display:flex;align-items:center;gap:0.75rem;font-size:1rem;color:var(--dark);}
    .plan-features li:before{content:'✓';color:var(--success);font-weight:700;font-size:1.25rem;}
    .plan-features li.disabled:before{content:'✗';color:var(--warning);}
    .plan-features li.disabled{color:#a0aec0;}
    .plan-cta{width:100%;padding:1.25rem 2rem;border:none;border-radius:0;background:var(--primary);color:white;font-size:1.1rem;font-weight:600;cursor:pointer;transition:all .2s;margin-top:1.5rem;}
    .plan-cta:hover{background:var(--primary-dark);}
    .plan-cta.popular{padding:1.5rem 2rem;font-size:1.2rem;background:linear-gradient(135deg,var(--primary),var(--primary-dark));}
    .popular-badge{position:absolute;top:-15px;left:50%;transform:translateX(-50%);background:var(--primary);color:white;padding:0.25rem 1.5rem;border-radius:50px;font-size:0.75rem;font-weight:600;}
    .features-section{padding:6rem 5%;background:white;}
    .container{max-width:1200px;margin:0 auto;}
    .section-header{text-align:center;margin-bottom:4rem;}
    .section-title{font-size:2.5rem;font-weight:700;color:var(--dark);margin-bottom:1rem;}
    .features-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));gap:2rem;}
    .feature-card{padding:2.5rem;border-radius:20px;background:var(--light);text-align:center;transition:all .2s;border:1px solid rgba(102,126,234,.1);}
    .feature-card:hover{transform:translateY(-5px);}
    .feature-icon{font-size:4rem;margin-bottom:1.5rem;background:var(--primary);color:white;width:80px;height:80px;border-radius:20px;display:flex;align-items:center;justify-content:center;margin-left:auto;margin-right:auto;}
    .footer{padding:4rem 5% 2rem;background:var(--dark);color:white;}
    .footer-content{max-width:1200px;margin:0 auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(250px,1fr));gap:2rem;}
    @media(max-width:768px){.pricing-container{gap:1.5rem;}.plan-header{padding:2rem 1.5rem;}.plan-features{padding:2rem 1.5r



<body>
  <nav class="navbar">
    <div class="nav-container">
      <a href="/" class="logo">Insight Hunter</a>
      <a href="/signup.html" class="cta-button">Get Started</a>
    </div>
  </nav>

  <section class="hero">
    <h1>Simple Pricing for Powerful Insights</h1>
    <p>Choose the plan that grows with your business. Cancel anytime.</p>
  </section>

  <div class="pricing-container">
    <div class="plan-card">
      <div class="plan-header">
        <div class="plan-name">Starter</div>
        <div class="plan-price">$29<small>/mo</small></div>
        <div class="plan-period">billed monthly</div>
      </div>
      <ul class="plan-features">
        <li>Monthly forecasting</li>
        <li>10 data sources</li>
        <li>Email reports</li>
        <li>Basic benchmarks</li>
        <li class="disabled">Priority support</li>
      </ul>
      <button class="plan-cta" onclick="checkout('starter')">Choose Starter</button>
    </div>

    <div class="plan-card" style="position:relative;">
      <div class="popular-badge">Most Popular</div>
      <div class="plan-header">
        <div class="plan-name">Growth</div>
        <div class="plan-price">$79<small>/mo</small></div>
        <div class="plan-period">billed monthly (save 20% yearly)</div>
      </div>
      <ul class="plan-features">
        <li>Weekly forecasting</li>
        <li>Unlimited data</li>
        <li>Real-time dashboard</li>
        <li>Advanced benchmarks</li>
        <li>Priority Slack support</li>
        <li>Custom integrations</li>
      </ul>
      <button class="plan-cta popular" onclick="checkout('growth')">Choose Growth</button>
    </div>

    <div class="plan-card">
      <div class="plan-header">
        <div class="plan-name">Enterprise</div>
        <div class="plan-price">$199<small>/mo</small></div>
        <div class="plan-period">custom quote</div>
      </div>
      <ul class="plan-features">
        <li>Everything in Growth</li>
        <li>White-label reports</li>
        <li>Dedicated analyst</li>
        <li>API access</li>
        <li>Custom workflows</li>
        <li>24/7 phone support</li>
      </ul>
      <button class="plan-cta" onclick="checkout('enterprise')">Contact Sales</button>
    </div>
  </div>

  <section class="features-section">
    <div class="container">
      <div class="section-header">
        <h2 class="section-title">Everything you need to scale</h2>
      </div>
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">📈</div>
          <h3>AI Forecasting</h3>
          <p>Predict cash flow, revenue, and expenses with 95% accuracy using your historical data.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">⚡</div>
          <h3>Real-time Alerts</h3>
          <p>Get notified instantly when KPIs deviate from benchmarks or forecasts.</p>
        </div>
        <div class="feature-card">
          <div class="feature-icon">📊</div>
          <h3>Board Reports</h3>
          <p>Beautiful, investor-ready reports generated automatically every week.</p>
        </div>
      </div>
    </div>
  </section>

  <script>
    function checkout(plan) {
      const token = localStorage.getItem('token');
      if (!token) {
        return window.location.href = '/signup.html?plan=' + plan;
      }
      
      // Stripe or Lemon Squeezy integration
      const planData = {
        starter: { price: 2900, name: 'Starter Monthly' },
        growth: { price: 7900, name: 'Growth Monthly' },
        enterprise: { price: 19900, name: 'Enterprise Monthly' }
      };
      
      const checkoutData = planData[plan];
      window.location.href = `/checkout.html?plan=${plan}&price=${checkoutData.price}&name=${checkoutData.name}`;
    }
  </script>
</body>


</Layout>
