---
export interface Props {
  variant?: 'tan' | 'success' | 'warning' | 'danger' | 'info' | 'role';
  class?:   string;
}
const { variant = 'tan', class: extraClass = '' } = Astro.props;
---
<span class={`badge badge--${variant} ${extraClass}`}>
  <slot />
</span>
